/** Imágenes de galería del producto cuando no tiene atributo de color. */
export const MAX_PRODUCT_IMAGES = 4;

/** Imágenes que se pueden relacionar con cada color del atributo de color. */
export const MAX_IMAGES_PER_COLOR = 2;

/** Tope del campo "Cantidad" (unidades disponibles). */
export const MAX_QUANTITY = 1000;

/** Precio máximo en pesos colombianos (100 millones). */
export const MAX_PRICE = 100_000_000;

/** Piezas de un producto vendido como set ("Set x N"): de 2 a 10. */
export const MIN_BUNDLE_SIZE = 2;
export const MAX_BUNDLE_SIZE = 10;
export const MAX_BUNDLE_LABEL_LENGTH = 20;

/** Longitudes máximas de los campos de texto. */
export const MAX_TITLE_LENGTH = 30;
export const MAX_REFERENCE_LENGTH = 30;
export const MAX_DESCRIPTION_LENGTH = 500;
export const MAX_SPEC_KEY_LENGTH = 15;
export const MAX_SPEC_VALUE_LENGTH = 30;
